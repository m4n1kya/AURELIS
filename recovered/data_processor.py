"import os\nimport json\nimport re\nfrom typing import Dict, Any, List\nfrom datetime import datetime, timedelta\n\nclass DataProcessor:\n    def __init__(self, llm_client, data_dir: str):\n        self.llm = llm_client\n        self.data_dir = data_dir\n        \n    def extract_image_amount(self, image_id: str) -> float:\n        image_path = os.path.join(self.data_dir, \"media\", \"images\", f\"{image_id}.png\")\n        if not os.path.exists(image_path):\n            return 0.0\n            \n        prompt = \"Extract the total amount or main financial amount shown in this image. Return a structured JSON.\"\n        schema = {\"type\": \"OBJECT\", \"properties\": {\"amount\": {\"type\": \"NUMBER\"}}}\n        \n        result_text = self.llm.call_gemini(prompt, image_path, json_schema=schema)\n        try:\n            data = json.loads(result_text)\n            return float(data.get(\"amount\", 0.0))\n        except Exception:\n            return 0.0\n\n    def is_message_potentially_relevant(self, text: str, request_date: datetime.date, sent_at_str: str) -> bool:\n        # Date filter\n        if sent_at_str and str(sent_at_str) != 'nan':\n            # Handle ISO 8601 strings like 2025-07-29T09:30:00Z\n            try:\n                sent_date = datetime.strptime(str(sent_at_str)[:10], \"%Y-%m-%d\").date()\n                max_date = request_date + timedelta(days=90)\n                if sent_date > max_date:\n                    return False\n            except Exception:\n                pass\n                \n        keywords = [\n            'change', 'increase', 'decrease', 'cancel', 'end', 'approved', 'pending', \n            'new', 'update', 'refund', 'settle', 'naik', 'turun', 'berubah', 'batal', \n            'berakhir', 'resume', 'reduces', 'adjustment', 'reimbursement', 'reimbursed',\n            'confirmed', 'dikonfirmasi', 'disetujui'\n        ]\n        text_lower = text.lower()\n        has_kw = any(kw in text_lower for kw in keywords)\n        return has_kw\n\n    def deterministic_pars
<truncated 3648 bytes>