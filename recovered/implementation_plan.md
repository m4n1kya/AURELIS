"# Standalone AURELIS Project Conversion Plan\n\nThe goal is to scrub all HackerRank competition traces from the codebase, repositioning AURELIS as a pristine, standalone fintech AI open-source project created from absolute zero.\n\n## Proposed Changes\n\n### 1. Directory Restructuring\n- **Rename Root Folder**: Rename `hackerrank-orchestrate-september26` to `AURELIS`.\n- **Relocate Engine**: Move all files currently inside `code/` to the root directory or a new `engine/` directory to flatten the structure. Let's move them to a new `engine/` directory for clarity, alongside `api/` and `frontend/`.\n\n### 2. Trace Removal (Scrubbing)\n- **Delete HackerRank Files**: Remove `problem_statement.md`, `run_and_zip.ps1`, `output.csv`, `code.zip`, `transcript.txt`, `log.txt`, `AGENTS.md`, and `CLAUDE.md`.\n- **Clear Caches**: Delete all `__pycache__` folders and `.next` build caches.\n- **Remove References**: Scan and replace any remaining occurrences of the word \"hackerrank\" in `api/main.py`, the dataset paths, and code comments.\n\n### 3. Open Source Foundation\n- **Git Reset**: Delete the existing `.git` directory and run `git init` to initialize a brand new, clean git history.\n- **New README.md**: Rewrite the `README.md` to look like a premium open-source project. It will include:\n  - Architecture overview (Engine, API, Frontend)\n  - Cloning and Setup instructions (`npm install`, `pip install -r requirements.txt`)\n  - Running instructions\n- **Requirements File**: Generate a `requirements.txt` containing the necessary Python packages (`pandas`, `google-generativeai`, `fastapi`, `uvicorn`, `python-dotenv`).\n\n### 4. Code Adjustments\n- If the `code/` folder is renamed to `engine/`, update the `api/main.py` imports and `sys.path` to reflect the new structure.\n- Update `dataset/` relative paths inside the engine to ensure they resolve correctly after the folder move.\n\n## User Review Required\n> [!IMPORTANT]\n> The current root folder `hackerrank-orchestrate-september26` will be renamed to `AURELIS`. If a folder
<truncated 187 bytes>