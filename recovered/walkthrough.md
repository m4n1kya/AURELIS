"# AURELIS: Next.js + FastAPI Premium Architecture\n\nThe AURELIS dashboard has successfully transitioned out of Streamlit into a fully production-grade, two-layer web application—while guaranteeing absolute isolation of the HackerRank Python competition environment.\n\n## The Dual-Layer Architecture\n\n1. **HackerRank Core (`code/`)**\n   - The competition directory remains 100% untouched. It executes purely in Python, without Node, npm, React, FastAPI, or uvicorn. \n   - Test `python code/main.py` directly to see the deterministic engine generate `output.csv` exactly as designed.\n\n2. **The Premium Product (`frontend/` & `api/`)**\n   - **FastAPI Bridge**: Operating independently out of `api/main.py`, it securely scopes into the `code/` module to invoke the `Evaluator` using the new `aurelis_service.py` wrapper, ensuring the React layer never hallucinates financial logic.\n   - **Next.js Frontend**: The application was bootstrapped in `frontend/` using React, TypeScript, and Tailwind CSS. The interface completely embodies the \"luxury fintech\" visual direction using strict `Inter` typography, subdued graphite backgrounds, and precise Recharts data visualization.\n\n## How to Test\n\nEverything is running locally in the background!\n\n**Product Path**\n- **Frontend**: [http://localhost:3000](http://localhost:3000)\n- **API (FastAPI)**: [http://localhost:8000/api/health](http://localhost:8000/api/health)\n\nUse the built-in demo buttons on the Next.js sidebar to instantly load:\n- Affordable Now (Req 02)\n- With Plan (Req 03)\n- Not Affordable (Req 05)\n- Evidence Case (Req 21)\n\nAll displayed numbers, dates, payment plans, and evidence status flags originate completely from the actual deterministic engine via the API. No sample answers are hardcoded.\n\n> [!IMPORTANT]\n> **HackerRank Submission Safety**\n> When packaging your final solution to the HackerRank platform, simply zip the `code/` directory. The `frontend/` and `api/` directories are securely isolated as presentation components and are not entangl
<truncated 38 bytes>