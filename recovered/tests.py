"import pytest\nfrom finance import Event, FinancialState\nfrom evaluator import Evaluator\nimport pandas as pd\nfrom datetime import datetime, date\n\nclass MockExchangeManager:\n    def convert(self, amount, d, f, t):\n        return amount\n\nclass MockProfile:\n    def __init__(self):\n        self.data = {\n            'home_currency': 'USD',\n            'current_available_balance': '1000',\n            'minimum_balance_to_keep': '200',\n            'expense_categories_to_protect': 'rent|groceries',\n            'expense_categories_user_is_willing_to_reduce': '',\n            'expense_categories_user_is_willing_to_stop': '',\n            'payment_methods_user_will_consider': 'full_payment|installments|wait',\n            'max_installment_months': '12'\n        }\n    def __getitem__(self, key):\n        return self.data[key]\n    def get(self, key, default=''):\n        return self.data.get(key, default)\n\ndef test_future_confirmed_income():\n    profile = MockProfile()\n    ev_row = pd.Series({\n        'event_id': 'e1', 'event_type': 'income', 'direction': 'credit', 'amount': '500', \n        'currency': 'USD', 'event_date': '2024-03-10', 'settlement_date': '2024-03-10', \n        'status': 'scheduled', 'category': 'salary'\n    })\n    ev = Event(ev_row, MockExchangeManager(), 'USD')\n    state = FinancialState('u1', profile, [ev], [], MockExchangeManager())\n    # Test valid cashflow inclusion\n    assert state.is_valid_cashflow(ev) == True\n    timeline = state.build_forecast(date(2024, 3, 1), 30)\n    # Check if +500 is on 2024-03-10\n    day_net = next((x['net'] for x in timeline if x['date'] == date(2024, 3, 10)), 0)\n    assert day_net == 500.0\n\ndef test_cancelled_events():\n    profile = MockProfile()\n    ev_row = pd.Series({\n        'event_id': 'e1', 'event_type': 'expense', 'direction': 'debit', 'amount': '100', \n        'currency': 'USD', 'event_date': '2024-03-05', 'settlement_date': '2024-03-05', \n        'status': 'cancelled'\n    })\n    ev = Event(ev_row, MockExchangeManager(), 'USD
<truncated 821 bytes>